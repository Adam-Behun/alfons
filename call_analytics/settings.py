# Env vars, API keys
# Central configuration file for environment variables and API keys.
# Loads settings for databases, STT providers, and ML models.
# Uses libraries like pydantic for validation and security.

# Analytics schema
# Defines SQL schemas (e.g., PostgreSQL) for structured analytics data.
# Includes tables for recordings, analytics, and patterns.
# Supports hybrid queries with MongoDB for vector data.